<?xml version="1.0" encoding="UTF-8"?>
<!--
Log4j2 configuration file.
This file controls the logging behavior of the application, including log levels,
output destinations (appenders), and message formatting.
-->
<Configuration status="WARN">
    <Properties>
        <Property name="log-path">logs</Property>
        <Property name="log-pattern">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
    </Properties>
    <Appenders>
        <!-- Console Appender: For logging to the standard output -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${log-pattern}"/>
        </Console>
        <!-- File Appender: For logging to a file -->
        <File name="File" fileName="${log-path}/automation.log">
            <PatternLayout>
                <Pattern>${log-pattern}</Pattern>
            </PatternLayout>
        </File>
        <!-- Rolling File Appender: For creating rolling log files -->
        <RollingFile name="RollingFile" fileName="${log-path}/app.log"
                     filePattern="${log-path}/app-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout>
                <Pattern>${log-pattern}</Pattern>
            </PatternLayout>
            <Policies>
                <TimeBasedTriggeringPolicy />
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>
    <Loggers>
        <!-- Root Logger: Default logger for all classes -->
        <Root level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Root>
        <!-- Custom Logger for specific packages -->
        <Logger name="stepDefinitions" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RollingFile"/>
        </Logger>
    </Loggers>
</Configuration>
